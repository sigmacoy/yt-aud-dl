1. cd to your project (in my case)
``` bash
cd /Users/mccauleybacalla/SigMacoy/Code/VSCode_Repos/yt-aud-dl
```

<!-- tab + space -->

2. Do this. Assumed already: 
     1. Python already installed
     2. pip install flask inside venv.
``` bash
python3 -m venv venv
source venv/bin/activate
python server/app.py
```

## All in one terminal command (on my end)
``` bash
cd /Users/mccauleybacalla/SigMacoy/Code/VSCode_Repos/yt-aud-dl
python3 -m venv venv
source venv/bin/activate
python server/app.py
```